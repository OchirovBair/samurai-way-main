import {ActionsTypes, ProfilePageType, SidebarType} from "./types";
import {v1} from "uuid";

export const sidebarReducer = (state:SidebarType, action: ActionsTypes) => {
    switch (action.type) {
        default:
            return state
    }
}
